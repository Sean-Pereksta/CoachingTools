# CoachTools assets

Optional suite-wide visual assets belong here. A future wallpaper can be added as `wallpaper.jpg` and referenced from `shared/coachtools-theme.css`. Keep large images out of HTML and JavaScript; do not encode them as base64.
